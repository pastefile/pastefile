UPLOAD_FOLDER = "./tests/files/"
FILE_LIST = "./tests/uploaded_files_jsondb"
TMP_FOLDER = "./tests/tmp"
EXPIRE = 86400
DEBUG_PORT = 5000
# Disable or not /ls url
DISABLED_FEATURE = ''
# Keep URLs as plain md5 in tests; the dedicated test toggles this on
# explicitly to exercise the EXPOSE_EXTENSION=true path.
EXPOSE_EXTENSION = 'false'
